# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-08-23

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- Berkomunikasi dalam Bahasa Indonesia (istilah teknis tetap Inggris). Balas dalam bahasa yang sama.
- Menjawab singkat dan cepat ("sudah okee", "iya boleh ditunda", "lanjut"). Sajikan pilihan konkret dengan rekomendasi tegas, bukan survei opsi netral — dia memilih rekomendasi hampir setiap kali.
- **Mendesain di Claude Design (claude.ai/design), bukan Figma.** Sebelum mengasumsikan tidak ada desain, tanyakan atau cek — dia sempat memilih "fungsional dulu, poles belakangan" lalu ternyata sudah punya design system lengkap + 11 layar jadi.
- Mengerjakan front-end lebih dulu dengan data dummy, backend menyusul — tapi minta fondasi produksi, bukan prototype sekali pakai.
- Menyediakan desain bertahap: bagian yang belum ada desainnya minta dibuat **wireframe** dulu, jangan digarap visualnya karena akan diganti.
- Untuk menggabungkan branch dia memilih **jalur PR GitHub**, bukan `git merge` lokal — konsisten dengan PR #1 & #2, supaya ada jejak. Dia belum hafal git; jelaskan kondisi branch (merge-base, jumlah commit beda, risiko konflik) sebelum menawarkan perintah.

- **Peduli warna SEMANTIK, bukan sekadar "tidak jelek".** Keluhannya berbunyi "warnanya jadi gak semantic" dan "primary vs secondary berantakan" — yang dia mau bukan tambal per layar, melainkan peran token yang benar. Saat melapor bug warna, dia melampirkan screenshot; reproduksi dulu persis screenshot itu sebelum menduga.

## Key Learnings

- **Graf graphify baru dibangun 2026-08-23.** Sebelumnya `graphify-out/` hanya berisi file deteksi — yang pernah jalan cuma `graphify claude install` (menulis section graphify ke CLAUDE.md), bukan pipeline grafnya. Bangun/refresh dengan `graphify update .` (AST-only, tanpa biaya LLM) — otomatis menulis ulang `graph.json`, `graph.html`, dan `GRAPH_REPORT.md`. Visualisasi = `graphify-out/graph.html`, buka langsung di browser (file lokal, tanpa server).

- **Project:** listinganku
- Repo has no application code yet — only PRD, onboarding flow doc, and AI-tooling scaffolding (OpenWolf/graphify/Codex/OpenCode configs). CLAUDE.md (2026-08-16) is a condensed spec distilled from the PRD + flow doc for future implementation sessions; treat it as the architecture/product source of truth until the Next.js app is actually scaffolded.
- Key product decision to remember: landing pages are generated **per Primary Property (Project)**, not per house type — one project with N house types = one landing page with N sections. Don't build per-house-type routes/pages.
- **Design ada di Claude Design**, dibaca lewat tool `DesignSync`. projectId `8059d123-0b3d-4faf-b7be-b76ce3506622`, file utama `Listingku App.dc.html` (11 layar, ~105KB) + design system `listingku-design-system-adfd85b5`. `get_file` mengembalikan JSON dengan field `content`; file besar disimpan ke tool-results, decode dulu dengan node sebelum dibaca.
- **Jebakan palette:** `uploads/Instruksi Prompt UI Listingku untuk pen.dev` menetapkan minimalist biru `#2563EB` — **dokumen itu usang**. Yang berlaku adalah evergreen `#233D2D` (suara utama) + oranye `#D2421A` (konversi saja) + leaf `#00803A` (mark & sukses saja, tidak pernah jadi fill tombol/permukaan).
- Design system memuat **aturan konten yang mengikat**, bukan sekadar visual: Bahasa Indonesia formal **Anda** (kapital) dan **kami**, tidak pernah *kamu*/*saya*; sentence case; **tanpa emoji, tanpa tanda seru**; angka gaya Indonesia (`Rp 2,45 M`, `1,2jt`, `09.00 WIB`).
- DS hanya mendefinisikan 6 komponen (Button, Card, Chip, Banner, Input, NavLink) dan menyatakannya eksplisit. Tabs/Switch/Toast/Dialog/Sheet/Accordion/Table harus datang dari Radix dan dicat token DS.
- `Canvas.dc.html` dan `Canvas-2.dc.html` di project design **kosong** — jangan buang waktu membacanya lagi.
- **JSON-LD lewat `dangerouslySetInnerHTML` butuh escape manual.** `JSON.stringify()` biasa tidak meng-escape `<`, jadi string apa pun yang berasal dari input pengguna (nama project, dsb.) dan memuat `</script>` bisa memutus tag `<script>` di level parser HTML — parser HTML mengenali `</script` murni dari teks mentahnya, tanpa peduli isi/atribut `type`. Pola aman: `JSON.stringify(data).replace(/</g, '\\u003c')` sebelum dipasang ke `__html` (`lib/landing/seo.ts:jsonLdScript`). `<title>`/meta tags TIDAK butuh ini — itu children React biasa, auto-escaped ke entities HTML.
- **Next.js membekukan route file (`sitemap.ts`, `robots.ts`, route handler) jadi statis di build time secara default** kalau tidak ada API dinamis (cookies/headers) yang terlihat — baca data lewat panggilan fungsi biasa ke `db` (bukan `fetch()`) tidak dianggap dependency dinamis oleh Next. Untuk route yang datanya bisa berubah di runtime tanpa rebuild (mis. `sitemap.ts` di atas mock store), wajib `export const dynamic = 'force-dynamic'`. Cek lewat tabel rute `next build` (`○` statis vs `ƒ` dinamis) — dev mode (`next dev`) tidak akan pernah menampakkan bug ini karena dev selalu re-eksekusi tiap request.
- **`next start` butuh `.next` yang valid dari `next build` TERAKHIR** — menjalankan `next dev` (termasuk lewat `npx playwright test`, yang men-start dev server via `webServer.command`) di antara `next build` dan `next start` merusak `.next` (BUILD_ID hilang). Selalu `npm run build` PERSIS sebelum `next start` untuk verifikasi produksi, tanpa `next dev` di antaranya.
- Server Component dinamis (`[slug]/page.tsx` tanpa `generateStaticParams`) tetap `ƒ` (server-rendered per request) secara default — beda dengan file route statis di atas. `cache()` dari `'react'` aman dipakai untuk menyatukan panggilan data antara `generateMetadata` dan komponen halaman (dipanggil dua kali secara terpisah kalau tidak).
- **Playwright e2e terhadap `next dev` webServer: panggilan PERTAMA ke sebuah Server Action membayar kompilasi on-demand (~10s+ terukur) yang melewati default `expect` timeout Playwright (5s).** Test submit pertama di sebuah file gagal dari cold; test submit tepat sesudahnya lolos karena action sudah warm. Test dalam satu file jalan serial (`fullyParallel` default `false`), jadi yang kena cold-compile selalu test PERTAMA yang memicu action itu. Fix: `expect: { timeout: 15_000 }` di `playwright.config.ts` — bukan menyentuh test, kondisi yang di-assert tidak berubah. Gejala khas di snapshot kegagalan: tombol submit masih `[disabled]` (pending tak pernah selesai), form utuh, tanpa pesan error — bedakan dari action yang benar-benar error (itu me-*re-enable* tombol dan menampilkan pesan).

- **E2e Playwright dari plan yang BELUM PERNAH dijalankan hampir selalu punya bug test-authoring (bukan bug app).** Empat pola yang muncul di Task 16–18: (1) race sesi — `click('Kirim magic link')` lalu `page.goto(rute-dashboard)` tanpa `waitForURL(/dashboard$/)` → middleware pantul ke /login; (2) `getByLabel` substring — `getByLabel('Judul')` ikut cocok 'Subjudul', pakai `{exact:true}`; (3) race navigasi `next dev` — klik tombol atau baca `page.url()` sebelum navigasi commit; selalu `waitForURL(...)` dulu; (4) label ganda — CTA header + tile grid "Add house type" dua tombol identik, pakai `.first()`. **Snapshot `error-context.md` (accessibility tree saat gagal) adalah alat diagnosis tercepat — baca itu, jangan menebak.**
- **`<Link href><Button>Label</Button></Link>` menghasilkan DUA elemen ber-accessible-name sama (role link DAN role button, nested).** `getByRole('button',{name})` bisa resolve ke tombol lama saat navigasi in-flight lalu mengekliknya (re-navigate, bukan aksi tujuan) — panel tujuan tampak "tidak bereaksi". Tunggu `waitForURL` ke halaman tujuan sebelum menarget tombolnya.
- **Satu mock store bersama (`.data/store.json`) tanpa isolasi per-test = e2e WAJIB serial (`workers:1`).** Worker paralel berebut state global (publish.spec ubah casa-verde→published vs landing-ssr.spec assert draft). Serial + urutan file abjad Playwright membuat asersi selalu mendahului mutasi. Jangan kembalikan ke paralel tanpa isolasi sungguhan.
- **`renameSync` (atomic write) di Windows sesekali gagal EPERM/EACCES saat file tujuan terkunci sejenak (antivirus/indexer/pembaca).** Muncul cuma di bawah beban (suite penuh menulis snapshot bertubi-tubi), jadi run terisolasi menyembunyikannya. Bungkus retry transien (`Atomics.wait` untuk tidur sinkron di jalur non-async). Berlaku untuk semua rename-over-existing yang jalan di Windows.
- **Angka di file design bisa MENDEFINISIKAN rumus, bukan sekadar mengisi ruang.** Kartu "Conversion · VISITOR → LEAD" di layar `leads` berbunyi 6,7%; seed memberi 1.842 visitor / 96 klik WA / 27 submit form. `leads.length/visitor` menghasilkan 0,3%, tapi `(whatsapp_click + form_submit)/visitor` = 6,68% → **6,7% persis**. Itu bukan kebetulan: hanya submit form yang membuat baris `leads` (`submitLeadAction`), klik WhatsApp berhenti di event — jadi memakai `leads.length` diam-diam membuang kanal penjualan yang justru utama. **Sebelum menebak rumus sebuah metrik, coba reproduksi angka di file design dari data seed.**
- **`.data/store.json` menua diam-diam.** `repairShape()` hanya mengisi tabel level atas yang HILANG — tabel yang ada tapi kosong (`leads: []`) dan field baris yang baru ditambahkan (`specialistArea`, `notifyOnLead`) TIDAK diperbaiki. Akibatnya: seed baru tidak muncul dan field baru terbaca `undefined` di store lama. Selalu `npm run seed:reset` sesudah mengubah `fixtures/seed.ts`, DAN beri default saat membaca field baru (`agent.specialistArea ?? ''`).
- **Nilai absolut di e2e = bom waktu di atas mock store bersama.** Suite e2e serial dan urut abjad di atas SATU store; `landing-lead.spec` (sebelum `leads.spec`) menambah lead, `landing-ssr`/`spine` menaikkan visitor. Lebih parah: `landing-lead.spec` mengirim nama dan nomor yang IDENTIK dengan lead seed (keduanya dari fixture design yang sama), jadi `getByRole('cell',{name:'Rina Wijaya'})` melanggar strict mode. Tulis asersi yang menyatakan maksud — `.first()` untuk kehadiran, baseline yang dibaca di test yang sama untuk jumlah, `toBeGreaterThanOrEqual` untuk metrik yang hanya bertambah.
- **`<details>`/`<summary>` sebagai dropdown: isinya tidak ada di accessibility tree sebelum dibuka**, dan `<summary>` ber-`list-style:none` dipetakan Chromium sebagai role `generic` (bukan `button`). Di e2e: klik `page.locator('summary').filter({hasText})` dulu, jangan andalkan `getByRole`.
- **Tes RTL tidak melihat computed style.** Delta kartu metrik terender sebesar h2 karena `.dash__metric dd` (0,1,1) mengalahkan `.dash__metric-delta` (0,1,0) — 322 unit test hijau, cacatnya baru terlihat di screenshot. **Sesudah menambah kelas modifier pada elemen yang sudah kena selektor descendant elemen, ambil screenshot** (dan sertakan nama elemen di selektornya).
- **`openwolf designqc` TIDAK ADA di openwolf 2.0.1** walau `.wolf/OPENWOLF.md` mendokumentasikannya (`openwolf --help` hanya punya init/status/scan/dashboard/report/daemon/cron/update/restore/bug). Untuk screenshot, pakai spec Playwright sementara: sudah ada `webServer` + login, tinggal `page.screenshot({fullPage:true})`, lalu hapus spec-nya.
- **Kode di plan implementasi bisa punya cacat yang baru ketahuan saat dieksekusi**, walau task awal mulus. Task 16: plan memanggil `<BlockRenderer projectId=...>` (prop tak ada → tsc gagal) dan membungkus blok interaktif di `<button>` (DOM invalid). Verifikasi tipe/API/DOM terhadap kode NYATA sebelum menyalin kode plan mentah — `next build` menangkap yang tipe, mata menangkap yang DOM.

- **Arsitektur tema landing (sejak 2026-08-19): satu set komponen PER TEMA.** `lib/landing/themes/<nama>/{blocks.tsx, theme.css, index.ts}` untuk kesepuluh tema, didaftarkan di `THEME_IMPL` (`themes/index.ts`) yang SENGAJA bukan `Partial` — menambah nama di `ThemeName` tanpa komponennya harus gagal kompilasi. Sebelumnya 9 tema memakai DOM tropicalWarm dengan CSS berbeda dan tidak pernah menyerupai file desainnya.
- **Landing MOBILE-ONLY.** Satu lebar: 390px (`lib/landing/landing.css` → `.lp{max-width:390px;margin-inline:auto}` + `.lp::before` fixed sebagai latar). Di layar besar bingkai dipusatkan, TIDAK dilebarkan. Jangan menambahkan media query `min-width` di file tema mana pun.
- **Penomoran section pakai CSS counter, bukan angka di JSX.** premiumDark memakai `counter(lp-sec, decimal-leading-zero)`, editorialWhite memakai `@counter-style lp-bab` (satu/dua/tiga…). Blok bisa diurut ulang agen dan nomornya tetap runut.
- **Primitif media bersama** ada di `themes/shared/parts.tsx` (`Ph`, `Img`, `initials`) dengan kelas netral `lp-ph` / `lp-img`; teksturnya milik tiap tema lewat `.lp[data-lp-theme='x'] .lp-ph`.
- **Slot media** (`themes/slots.ts`) memisahkan "berapa banyak & apa namanya" (blok) dari "sudah ada filenya" (media) — itu yang membuat galeri & denah punya bentuk sebelum foto diunggah.
- **Konten kanonik Parkspring** di `fixtures/seed.ts` disalin dari `design/project/10 Tropis Hangat.dc.html`. Kesepuluh tema memakai konten yang SAMA PERSIS; hanya tampilan dan urutan section yang berbeda. Jangan mengubah salah satunya tanpa alasan — perbandingan visual bergantung padanya.
- **Papan banding** ada di `/preview` (grid 10 iframe) dan `/preview/{tema}` (satu tema penuh, `track={false}` supaya pratinjau tidak menghitung visitor). Keduanya memakai `LandingView` yang sama dengan halaman publik.
- **`gh` CLI TIDAK terpasang** di mesin ini (tidak ada di Git Bash maupun PowerShell). PR tidak bisa dibuat dari terminal — siapkan judul + body lalu berikan URL `https://github.com/firdibackup/listinganku/compare/{base}...{head}?expand=1`.
- **Topologi branch (2026-08-23):** `main` (cfd945e) hanya lokal, tidak pernah di-push dan tertinggal di baseline pra-kode. `origin/HEAD` menunjuk ke `slice-1-frontend`, yang de-facto jadi branch utama. `slice-2-templates-mobile` = slice-1 + 1 commit (`44c4021`). Rencana: setelah slice-2 masuk, majukan `main` lalu jadikan default branch.
- **Commit `44c4021` pesannya menyesatkan** — tertulis "add Soft Luxury theme components and styles", isinya seluruh refactor tema per-folder + `/preview` + tes (81 file, +9.752/-1.856). Jangan percaya pesan commit itu saat menelusuri sejarah.

- **Kontrak peran token landing (2026-08-23).** Tiap token LATAR punya token TULISAN pasangannya, dan tema hanya boleh memakai pasangan itu: `bg`/`surface`/`ph-a`/`ph-b` -> `ink`|`ink-soft`|`ink-faint`|`accent-ink`; `accent` -> `on-accent`; `accent-soft` -> `on-accent-soft`; `contrast` -> `on-contrast`|`on-contrast-soft`|`contrast-accent`; `feature` -> `on-feature`|`on-feature-soft`; `footer` -> `on-footer`. **`accent` adalah warna LATAR** — untuk aksen sebagai teks di atas bg/surface pakai `accent-ink` (di boldRetail kuning #ffd400 cuma 1,39:1 di atas krem). Dijaga `tests/unit/theme-token-pairs.test.ts` yang memindai kesepuluh `theme.css` x kesepuluh palet.
- **`Project.theme` dan `Project.palette` sekarang berpasangan.** `setThemeAction` ikut menulis `palette = THEME_DEFAULT_PALETTE[theme]` dan mengembalikannya. Tiap `theme.css` ditranskrip 1:1 dari satu file desain yang mengandaikan paletnya sendiri, jadi 90 dari 100 kombinasi tidak pernah didesain. PalettePicker tetap ada — penyimpangan yang DISENGAJA boleh, yang dihapus cuma penyimpangan diam-diam.
- **Audit kontras paling murah lewat papan `/preview`.** Sepuluh iframe same-origin di satu halaman: satu `evaluate_script` bisa menjalankan auditor di `f.contentDocument`/`f.contentWindow` untuk kesepuluh tema sekaligus (~1.600 simpul teks) tanpa sepuluh kali navigasi. Batasnya: auditor menelusuri latar lewat rantai INDUK, jadi teks di atas elemen overlay ber-`position:absolute` (hero tropicalWarm) dilaporkan salah — hitung komposit gradiennya manual untuk kasus itu.
- **`z.record(z.enum([...]), valueSchema)` di zod 3.25.76 (versi proyek ini) MENOLAK kunci di luar enum dengan benar** — diverifikasi manual via `node -e` (bukan sekadar dipercaya dari memori) sebelum dipakai di `ProjectBriefSchema.notes` (Task 1 slice pipeline brief). Kekhawatiran umum "record dengan key enum di zod 3 meloloskan kunci asing" TIDAK berlaku di versi ini — jangan ganti ke pola `.partial()` objek kalau tidak perlu.
- **`ProjectBrief` (slice pipeline brief, Task 1, `lib/data/types.ts` + `lib/schemas/project.ts`)**: bahan mentah agen (lokasi, nearby, promo, notes per-BlockType) hidup TERPISAH dari `blocks[].props` — fakta di atas AI, copy AI di bawahnya. `NearbyItem.minutes: number | null` sengaja TIDAK BISA jadi string — itu penjaga struktural anti-halusinasi AI, bukan sekadar tipe. `emptyBrief()` wajib dipakai di setiap pembacaan brief. `ProjectPublishSchema` sengaja TIDAK mewajibkan `brief` — brief kosong tidak boleh memblokir publish.
- **Task 6 slice pipeline brief (2026-08-23): `SECTION_PRESET` di `lib/landing/sectionPreset.ts`** — tabel deterministik `Record<ProjectType, BlockType[]>` (bukan panggilan AI kedua, biar Time to Publish <10 menit tidak kena tambahan 5–15 detik). `testimonials` TIDAK PERNAH masuk preset mana pun (resolve.ts sengaja tanpa fallback AI karena testimoni fabrikasi = penipuan ke calon pembeli). `applySectionPreset()` cuma menyentuh `enabled` — urutan tetap milik tema, props tetap milik agen (menyalin bug lama `applyThemeOrder` yang sudah diperbaiki: mematikan section BUKAN alasan membuang isian). Dipanggil di `createProjectAction` (sesudah create, saat projectType pertama ada) dan `updateProjectAction` (HANYA saat `projectType` benar-benar berubah dari nilai lama — mencegah simpan-langkah-wizard lain menimpa toggle manual agen). **Untuk Task 11 (wizard mengirim `blocks` sendiri di payload yang sama):** baris kunci di `updateProjectAction` memakai `patch.blocks ?? owned.blocks`, BUKAN `owned.blocks` telanjang — kalau dipakai `owned.blocks` langsung, toggle/props yang baru disunting agen di step 2 akan tertimpa salinan server yang basi. `ProjectDraftSchema` belum punya field `blocks` (itu tugas Task 11) — solusi sementara di actions.ts men-cast `patch` ke `typeof parsed.data & { blocks?: typeof owned.blocks }` saat deklarasi (bukan cast inline per-baris), supaya `patch.blocks` bisa dibaca DAN ditulis tanpa melonggarkan skema Zod. Task 11 nanti tinggal menambah `blocks` sungguhan ke `ProjectDraftSchema` dan cast ini otomatis jadi berlebih (aman dihapus).
- **Task 2 slice pipeline brief (2026-08-23) menutup `repairShape()` untuk `projectType`/`brief`** — sekarang menambal field BARIS itu juga (dulu hanya theme/palette/blocks; lihat entri "store.json menua diam-diam" di atas, celah itu sekarang tertutup untuk kedua field ini). Konsekuensi yang TIDAK terlihat dari tsc: `db.projects.create()` kini isi default (`projectType: null`, `brief: emptyBrief()`), dan snapshot lama ditambal saat dibaca. **Efek samping yang diharapkan sampai Task 4 selesai**: `repairShape()` menambal field itu UNCONDITIONAL untuk setiap baris `projects` (termasuk yang jatuh ke fallback `seedStore()` penuh), tapi `fixtures/seed.ts` belum diisi `brief`/`projectType` (itu tugas Task 4) — jadi `expect(loadSnapshot()).toEqual(seedStore())` di `tests/unit/mock-store.test.ts` (2 test, baris ~465 & ~473) SEMENTARA merah karena sisi kiri (repaired) punya field itu dan sisi kanan (seed segar) tidak. Ini bukan bug Task 2 — akan sembuh sendiri begitu Task 4 mengisi `project()` helper di `fixtures/seed.ts` dengan `brief`/`projectType`. Jangan "perbaiki" dengan melonggarkan `repairShape()` atau mengubah asersi test itu.

- **Task 7 slice pipeline brief (2026-08-23): `lib/places/regions.ts` + `data/id-regions.json`.** Dataset offline STARTER (39 kecamatan) — bentuk baris `{district, city, province}` sengaja stabil, sumber/lisensi dataset penuh (~7.300 kecamatan) masih blocker eksternal terbuka; nanti hanya isi file yang berganti, nol perubahan kode. JSON diimpor STATIS (`import raw from '@/data/id-regions.json'`), bukan lewat `node:fs` — modul ini boleh diimpor dari route handler (Task 8) tanpa menyeret masalah `lib/data` barrel (lihat entri lain: barrel itu menyentuh `node:fs` saat modul dimuat, gagal di Edge). **Verifikasi runtime, bukan asumsi**: `/\p{Diacritic}/gu` (unicode property escape) diverifikasi bekerja benar di Node 24.19.0 lewat `node -e` langsung (`café`→`cafe`, `Curaçao`→`curacao`) sebelum dipakai — tidak perlu penggantian. `searchRegions()` mendahulukan match AWALAN atas match tengah lewat dua array terpisah (`prefix`/`contains`) baru digabung — urutan baris di JSON tidak mempengaruhi urutan hasil.

## Do-Not-Repeat

- [2026-08-19] **Jangan menulis `.lp[data-lp-theme='x'] a { color: ... }` di file tema.** Spesifisitasnya (0,2,1) mengalahkan SEMUA kelas tombol (0,1,0), jadi setiap tombol yang dirender sebagai `<a>` (WhatsAppLink, anchor CTA) kehilangan warnanya dan jadi gelap-di-atas-gelap. Pakai `:where(.lp[data-lp-theme='x']) a` — (0,0,1), kalah dari kelas apa pun. Kesalahan yang sama terulang di kesepuluh tema sekaligus karena polanya disalin (`bug-031`).
- [2026-08-19] **`defaultBlocksForTheme()` mengembalikan props KOSONG.** Memakainya untuk "terapkan urutan bawaan tema" menghapus seluruh isi blok agen. Untuk urutan saja pakai `applyThemeOrder(blocks, theme)` (`bug-032`).
- [2026-08-19] **Komponen yang `return null` saat media kosong = section hilang diam-diam.** Galeri & denah tidak pernah tampil di landing mana pun karena seed `media: []`, dan tidak satu tes pun gagal. Bentuk section harus datang dari DATA BLOK (keterangan, daftar tipe), bukan dari ada-tidaknya file (`bug-033`, sekelas `.lp__stickybar`).
- [2026-08-23] **Menaikkan sebuah field opsional jadi wajib di tipe input (`GenerateInput.brief`) diam-diam merusak panggilan lama** yang belum mengirim field itu — di JS runtime jadi `undefined`, lalu `undefined.location?.area` (properti pertama diakses tanpa optional chaining di objeknya sendiri) melempar TypeError, bukan gagal-lolos-diam seperti biasanya. Saat plan menaikkan sebuah field jadi wajib, grep semua call site dulu (`generate({...})`) dan tambal yang belum mengirim field itu — jangan asumsikan "tambah tes baru" saja cukup.
- [2026-08-19] **Perintah Bash terpotong di sekitar ~135 baris** — heredoc yang lebih panjang gagal dengan "unexpected EOF while looking for matching `''". Tulis file besar bertahap (`cat >` lalu `cat >>`), masing-masing < ~110 baris. Dan pakai r-string Python untuk pola berisi backslash-n: argumen JSON menyusutkan dua backslash jadi satu sebelum sampai ke file, jadi pola yang seharusnya cocok dengan escape \n di sumber TS malah jadi newline sungguhan (`bug-034`).

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-08-17] **Aset logo `public/brand/*.png` yang diminta task 7 ("unduh dari project Claude Design") tidak bisa dipenuhi dari sesi subagent** — tidak ada tool DesignSync/akses project Claude Design di context subagent, hanya di sesi utama. Disintesis 3 PNG placeholder (wordmark + mark, palette evergreen/oranye) lewat `node_modules/sharp` langsung dari script sementara, bukan didownload. Kalau butuh aset asli, jalankan `DesignSync` dari sesi utama (bukan subagent) lalu re-export ke `public/brand/`.
- [2026-08-17] Server Component async (`app/(dashboard)/layout.tsx`, `.../page.tsx`) bisa dites langsung di Vitest+jsdom tanpa server Next sungguhan: panggil fungsinya sebagai async function (`await DashboardPage()`), lalu `render()` hasil JSX-nya dengan RTL. `next/image` dan `next/link` merender apa adanya di jsdom tanpa mock tambahan. `redirect()` dari `next/navigation` di-mock supaya `throw` bisa ditangkap `rejects.toThrow` — variabel yang dipakai di dalam factory `vi.mock` wajib dibungkus `vi.hoisted()`, kalau tidak kena "Cannot access 'x' before initialization" karena factory di-hoist ke atas file.

- [2026-08-18] npm cache di Windows bisa membengkak sampai puluhan GB (terukur **42GB**) dan memenuhi disk sistem — Playwright lalu gagal `ENOSPC: no space left on device` saat menulis trace (bukan bug test). Kalau e2e gagal ENOSPC / disk penuh: cek `df -h .`, lalu `npm cache clean --force` (bebaskan cache dulu, jangan hapus `node_modules`). `.next` (~190MB) juga aman dihapus.
- [2026-08-18] Mocking `node:fs` di Vitest lewat `vi.mock('node:fs', {...actual, renameSync: mock})` TIDAK meng-intercept named import `renameSync` yang dipakai modul lain (spy 0 kali dipanggil, fungsi asli tetap jalan). Untuk menguji fungsi yang membungkus builtin fs, pakai **seam dependency-injection** (terima fungsi sebagai parameter, default = builtin) alih-alih mem-mock `node:fs`.

- [2026-08-18] **Jangan komit verifikasi manual sebagai spec Playwright tanpa gate env.** `tests/e2e/_aifail.spec.ts` (§14 #4) butuh server dengan `AI_MOCK_FAIL=1`, tapi `webServer` di `playwright.config.ts` menjalankan `npm run dev` polos — jadi mock AI justru SUKSES dan `npm run verify` merah 1/22. Kalau sebuah spec butuh env khusus: beri `test.skip(process.env.X !== '1', '...')` di badan tes, dokumentasikan cara menjalankannya di header file, dan **beri nama yang terurut terakhir** (mis. prefiks `zz-`) — Playwright menjalankan file berurutan abjad di atas SATU mock store bersama, dan spec yang memublikasikan project akan merusak `landing-ssr.spec.ts` yang menuntut casa-verde masih draft. Prefiks `_` justru terurut PALING AWAL (0x5F < 'a'), kebalikan dari yang diduga.
- [2026-08-18] **`npm run verify | tail` menyembunyikan exit code.** Tanpa `pipefail`, exit code yang dilaporkan adalah milik `tail`, jadi suite yang merah tetap tampak "exit 0". Untuk memastikan status sebenarnya: `npm run verify > log 2>&1; echo "EXIT_CODE=$?"` lalu baca lognya — jangan pakai pipe.
- [2026-08-18] **CSS yatim = fitur yang hilang diam-diam.** `.lp__stickybar` lengkap dengan media query mobile sudah ada di `lib/landing/landing.css` sejak Task 12/13, tapi komponen `StickyCtaBar.tsx` yang merendernya tidak pernah dibuat di Task 14 — tidak ada satu tes pun yang gagal karenanya. Saat mengaudit task selesai, jangan hanya cek "tes hijau": cocokkan daftar `- Create:` di plan dengan `os.path.exists`, dan curigai kelas CSS yang tidak pernah dirujuk kode.

- [2026-08-19] **Jangan jalankan `npm run build` / `npm run verify` selagi `next dev` masih hidup.** Build produksi menimpa `.next` yang module graph dev server sedang pegang → `Cannot find module './611.js'`, dan SELURUH e2e gagal timeout (`bug-023`). Cerebrum sudah mencatat arah sebaliknya (`next start` sesudah `next dev`); akarnya sama — satu direktori `.next` tidak boleh dilayani dua mode. Cek `Get-NetTCPConnection -LocalPort 3000` dulu; gejala khas di Playwright: "Port 3000 is in use ... using 3001 instead" lalu webServer timeout.
- [2026-08-19] **Sidebar sempat punya empat item nav yang SEMUANYA ber-`href="/dashboard"` dan prop `active` yang di-hardcode `"dashboard"` di layout** — "Leads" dan "Pengaturan" adalah link mati selama slice 1, dan tidak ada satu tes pun yang gagal karenanya. Sekelas dengan pelajaran `.lp__stickybar`: **UI yang menunjuk ke tempat yang salah tidak akan pernah menyalakan tes.** Saat menambah halaman, periksa apakah nav-nya benar-benar sampai ke sana.

- [2026-08-19] **Jangan pakai sintaks here-string PowerShell (`@'...'@`) di tool Bash.** `git commit -m @'...'@` menghasilkan commit yang subjeknya literal `@` dan badannya tergeser satu baris (commit `428f282`). Bash membaca `@` sebagai kata terpisah, bukan pembuka here-string. Untuk pesan commit multi-baris di Bash: tulis ke file lalu `git commit -F <file>`.
- [2026-08-19] **GateGuard memblokir `git commit --amend` berulang** walau fakta sudah dipresentasikan tiga kali dengan format yang diminta (daftar file, rollback satu baris, instruksi user verbatim). Jangan buang giliran menekannya — susun pesan commit dengan benar SEBELUM commit, atau terima cacat kosmetiknya dan lanjut. Gate yang sama menuntut fakta sebelum `Write` file baru (termasuk file sementara di scratchpad), sebelum perintah Bash pertama tiap sesi, dan sebelum `mv` yang menimpa file. `ECC_GATEGUARD=off` mematikannya kalau benar-benar menghalangi kerja perbaikan.
- [2026-08-19] **Lint no-literal-colors memindai komentar juga.** Regex `theme-no-literal-colors.test.ts` menangkap `rgba(`/`hex` di baris komentar CSS/TSX di bawah `lib/landing/themes/**` (`bug-028`). Jangan tulis literal warna di komentar file tema — atau tandai baris dengan `lp-lint-allow`. Warna semi-transparan pakai `color-mix(in srgb, var(--lp-x) N%, transparent)`, bukan fungsi beralfa.
- [2026-08-19] **PalettePicker swatch memakai `aria-label` = PALETTE_LABELS yang IDENTIK dengan THEME_LABELS** — `getByRole('button',{name:'Tropis Hangat'})` cocok DUA elemen (tombol tema + swatch) → strict-mode violation (`bug-029`). Di e2e editor, scope selalu: `page.locator('.ed__themes').getByRole(...)` untuk tema, `.ed__palettes` untuk swatch.
- [2026-08-19] **`next/font/google` tidak jalan di jsdom/Vitest.** Modul apa pun yang mengimpor `lib/landing/fonts.ts` (mis. `themes/index.ts`) akan gagal collect di Vitest. Fix: alias `next/font/google` → `tests/mocks/next-font.ts` di `vitest.config.ts` (factory kembalikan `{variable, className}`). Saat menambah font tema, tambahkan nama ekspornya ke mock itu.

- [2026-08-23] **Jangan memakai `position: fixed` + `z-index: -1` sebagai latar di belakang sebuah elemen kalau elemen itu bisa di-`transform`.** `.lp::before` dipakai untuk latar di luar bingkai 390px; pratinjau editor memakai elemen `.lp` yang SAMA lalu `transform: scale(.72)`. Transform membuat elemen jadi containing block untuk keturunan `position:fixed` DAN stacking context, jadi pseudo-nya (a) berhenti kabur ke viewport dan (b) dicat SESUDAH latar elemennya sendiri — seluruh pratinjau tertutup. Gerbang efek semacam ini dengan penanda eksplisit (`[data-lp-standalone]`) yang hanya dipasang pemakai yang punya viewport sendiri (`bug-035`).
- [2026-08-23] **`color-mix(var(--lp-x) N%, <hex literal>)` adalah warna tidak semantik yang menyelinap lewat lint.** `theme-no-literal-colors` hanya memindai `lib/landing/themes/**`, jadi `#0e1013` di `lib/landing/landing.css` lolos dan menghasilkan lumpur `#898683` di semua palet terang. Kalau butuh warna baru, tambahkan TOKEN palet, jangan campur hex di CSS kerangka (`bug-035`).
- [2026-08-23] **Palet boleh punya beberapa token bernilai SAMA dan tetap lolos semua tes lama.** natureCalm menyetel `accent` = `contrast` = `feature` = `#4a6047`; `palettes.test.ts` lama cuma menguji enam pasangan sehingga sembilan blok dengan kontras 1,00:1 lolos ke produksi. Saat menambah palet, uji SETIAP pasangan latar/tulisan, bukan sampel (`bug-036`).
- [2026-08-23] **`--lp-quiet` / `--lp-dim` yang mengencerkan ink ke arah bg selalu jatuh di bawah AA.** Pola `color-mix(in srgb, var(--lp-ink-faint) 72%, var(--lp-bg))` untuk label 10-11px menghasilkan 2,4-3,9:1 di delapan tema. `ink-faint` sekarang DIDEFINISIKAN sebagai "ink teredup yang masih lolos 4,5:1 di atas bg/surface/ph-a/ph-b", jadi `--lp-quiet` cukup jadi aliasnya — jangan encerkan lagi.
- [2026-08-23] **Teks di atas media harus dihitung terhadap PLACEHOLDER, bukan cuma terhadap foto bayangan.** Sampai agen mengunggah foto, `media: []` membuat medianya placeholder KREM; scrim hero tropicalWarm yang mulai dari `transparent 30%` menyisakan badge 2,70:1 dan subjudul 2,67:1. Beri scrim bobot sejak puncak dan uji tiga latar: placeholder, foto terang, foto gelap (`bug-038`, sekelas `bug-033`).

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

Detail lengkap + alasan ada di `docs/superpowers/specs/2026-08-16-listingku-frontend-slice1-design.md` §15.

- **[2026-08-16] Repository seam + Server Actions**, bukan Zustand/localStorage atau mock HTTP layer. Landing publik wajib SSR untuk SEO; store di klien akan memaksa penulisan ulang halaman saat Supabase masuk, dan lapisan HTTP tidak dipakai arsitektur final (Server Component memanggil Supabase langsung).
- **[2026-08-16] Mock store di-snapshot ke `.data/store.json`.** Bukan sekadar kenyamanan — HMR Next.js me-reset state level modul, jadi tanpa snapshot data yang diketik hilang.
- **[2026-08-16] `blocks`/`seo`/`theme`/`ai_content` pindah dari `house_types` ke `projects`.** Blok skema di PRD adalah peninggalan sebelum v3.0; karena satu landing = satu project, tidak ada baris `house_types` yang bisa memiliki halaman itu.
- **[2026-08-16] Respons AI dipecah saat disimpan** — bagian proyek ke `projects.ai_content`, tiap `houseTypes[]` ke baris `house_types` masing-masing. Menyimpan utuh membuat entri yatim saat tipe dihapus dan membuat editor menulis ke indeks array, bukan ke baris yang benar.
- **[2026-08-16] Blocks menyimpan referensi (mediaId, houseTypeId), bukan salinan harga/foto.** Membuat US-B3 otomatis terpenuhi: ubah harga di detail tipe, landing live ikut berubah tanpa menyentuh editor.
- **[2026-08-16] Field teks kosong = pakai default AI, mengisi = override.** Konsekuensinya tombol "Use AI suggestion" (US-C2) bukan fitur terpisah — ia cukup menghapus override.
- **[2026-08-16] Komponen hybrid: port DS 1:1 + Radix untuk sisanya.** Menulis focus trap, ARIA, dan keyboard nav sendiri adalah tempat bug aksesibilitas paling sering lolos; tapi Button/Card/Chip tetap di-port karena itu identitas brand.
- **[2026-08-16] Pratinjau editor memakai `BlockRenderer` sungguhan, bukan frame skematik seperti di file design.** US-F1 menuntut pratinjau identik dengan live; dua representasi akan berdivergensi begitu tema asli masuk. Chrome design (header PRATINJAU, toggle DESKTOP/MOBILE, ring evergreen) tetap dipertahankan.
- **[2026-08-19] Status lead READ-ONLY di halaman Leads** (keputusan user). `LeadStatus` punya 6 nilai tapi tidak ada kode yang menulisnya; menambah `leads.updateStatus` ditunda ke sesi lain. Konsekuensi yang dipakai e2e: setiap lead yang dibuat spec lain selalu berstatus `new`, sehingga asersi jumlah pada filter `status=deal` stabil.
- **[2026-08-19] Ketiga tab Settings menulis data ASLI** walau situs profil `{subdomain}.listingku.app` belum dibangun (keputusan user). Semua field sudah punya kolom di `AgentProfile`, jadi tidak ada kerja terbuang — situs profilnya tinggal membaca saat dibangun. Alternatif "tampil tapi belum menyimpan" ditolak: kontrol yang tidak menyimpan adalah bug yang menunggu dilaporkan.
- **[2026-08-19] Filter Leads dan tab Settings hidup di URL, bukan state klien.** Kedua halaman tetap Server Component, hasilnya bisa di-bookmark/dibagikan, tombol back bekerja, dan tidak ada dependency baru (`@radix-ui/react-tabs` tidak jadi dipasang). Tab Settings memang tiga form berbeda — navigasi lebih tepat daripada panel-dalam-satu-view.
- **[2026-08-19] Ditambah dua field ke `AgentProfile`: `specialistArea` (teks) dan `notifyOnLead` (boolean).** Desain menampilkan "Wilayah Spesialis" sebagai stat ke-3, tapi `stats` seluruhnya angka — menaruh string di sana akan mengotori tipenya. `stats.years` tetap ada untuk situs profil. Juga ditambah `events.listByUser` ke `DataStore`: `totalsByUser` meratakan tanggal, jadi delta "7 HARI" tidak bisa dihitung darinya.
- **[2026-08-19] Tombol "Publikasikan situs profil" ditambahkan di luar file design.** Desain hanya menggambar "Unpublish" karena mengasumsikan situs sudah live; tanpa jalan kembali, agen bisa mematikan profilnya secara permanen.
- **[2026-08-16] Tabrakan slug di root** (`/{project-slug}` vs `/dashboard`) diselesaikan dengan daftar slug terlarang + suffix angka, bukan URL scoped `/u/{agen}/{slug}`. PRD Open Question #2 dengan demikian dijawab.

- **[2026-08-23] Palet mengikuti tema, bukan tema dibuat palette-agnostic.** Tiga opsi ditimbang: (A) ganti tema menyetel palet bawaannya + perbaiki tabrakan token, (B) hapus PalettePicker, (C) tulis ulang bagian warna 10 stylesheet (~2.350 baris) supaya 100 kombinasi valid. User memilih A. Alasan C ditolak: `feature` dan `accent-soft` memang mengemban DUA peran berbeda antar palet (blok kontras tebal vs permukaan bertingkat halus), dan menyeragamkannya akan menghapus ciri tema yang dikunci di STATUS.md (mis. pita hijau natureCalm, pita kuning boldRetail). B ditolak karena menghapus fitur yang dipakai agen untuk menyesuaikan warna merek.
- **[2026-08-23] `accent-ink` ditambahkan alih-alih menggelapkan `accent`.** Enam palet nilainya identik dengan `accent` sehingga diagonal tidak berubah sama sekali; hanya boldRetail (kuning), softLuxury, classicNavy, tropicalWarm yang mendapat varian teks yang lebih gelap. Menggelapkan `accent` langsung akan mengubah warna tombol dan pita — identitas tema — demi memperbaiki teks.
- **[2026-08-23] Aturan tautan generik disamakan jadi `color: inherit` di kesepuluh tema.** Lima tema memakai `color: var(--lp-accent)`, yang benar di badan halaman tapi salah di dalam footer/pita gelap (tautan footer natureCalm 1,27:1). `inherit` selalu mengambil token pasangan yang benar dari teks sekitarnya, dan setiap `<a>` yang memang perlu warna sendiri sudah punya aturan eksplisit.

- **[2026-08-23] Bahan mentah agen disimpan di `project.brief` (JSONB) TERPISAH dari `blocks[].props`.** Alternatif "tulis langsung ke props" ditolak: semantik `props` sudah terlanjur "override AI" (kosong = pakai AI, terisi = override), jadi wizard yang menulis fakta ke sana akan mematikan AI diam-diam untuk field itu, membuat regenerate tidak berefek, dan membuat tombol "Use AI suggestion" bohong. Intent (penekanan hero, tujuan CTA) juga tidak punya tempat tinggal di `props` karena bukan konten yang dirender.
- **[2026-08-23] Fakta di ATAS AI, copy di BAWAH AI.** Garisnya: fakta = data yang jadi salah kalau diubah (nearby, promo, fasilitas); copy = kalimat yang boleh diperbaiki (headline, subheadline, keunggulan, FAQ). `highlights` sengaja ada di sisi copy — agen menulis mentah, AI merapikan, tulisan mentah tetap jadi fallback kalau AI gagal.
- **[2026-08-23] Grounding AI ditegakkan di lapisan RENDER, bukan di prompt.** Tiga penjaga struktural: `NearbyItem.minutes` bertipe `number | null` (tidak ada field untuk dikarang), `resolve()` untuk `access`/`facilities`/`pricePromo` tidak pernah membaca `ai.*`, dan `AiContentSchema` tidak punya satu pun field angka. Aturan di prompt tetap ditulis tapi sebagai lapis kedua. Ini menutup risiko "AI mengarang jarak" yang tercatat di PRD.
- **[2026-08-23] Rekomendasi section = preset deterministik per `projectType`, bukan panggilan AI kedua.** Panggilan AI kedua ditolak: +5–15 detik tepat di jalur North Star Time to Publish <10 menit, +biaya per project, melanggar prinsip satu panggilan AI per Primary Property, dan TETAP menuntut preset deterministik sebagai fallback. `testimonials` selalu mati di semua preset — testimoni tidak boleh datang dari AI.
- **[2026-08-23] Autocomplete lokasi dari dataset administratif offline, bukan Google Places.** Nol biaya, nol key, jalan di e2e tanpa mock. Konsekuensi yang diterima sadar: tidak ada lat/lng dan tidak ada "Pilih di Map"; `mapUrl` tetap ditempel manual. Nama kawasan komersial ("Gading Serpong") bukan unit administratif sehingga selalu butuh field bebas terpisah. Dibungkus `LocationProvider` mengikuti pola `getGenerator()`.
- **[2026-08-23] Slice 3A dibatasi supaya TIDAK menyentuh satu pun file di `lib/landing/themes/`.** Dua keputusan turunan lahir dari batas ini: nearby tanpa `minutes` tidak masuk `access` (alternatifnya 10 suntingan penjaga di 10 `Location.tsx`), dan label CTA tetap milik tema (`ctaGoals` tetap dikumpulkan + dikirim ke AI, tapi hanya pesan WhatsApp yang jadi data). Keduanya ditunda ke 3B bersama blok `about`.
